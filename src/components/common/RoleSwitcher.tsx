import React from 'react';

/**
 * Simulador de Rol RBAC - Retirado para Producción.
 * La gobernanza de roles es administrada exclusivamente por la sesión real de Supabase Auth.
 */
export const RoleSwitcher: React.FC = () => null;
